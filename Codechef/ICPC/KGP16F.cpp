#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;

int main()
{
	ll t;
	cin >> t;
	while(t--)
	{
		ll n, k;
		cin >> n >> k;
		ll i, a[i];
		for(i=0;i<n;i++)
		{
			cin >> a[i];
		}
		sort(a, a+n);
		for(i=2;i<n;i++)
		{

		}
	}
	std::getchar();
	cout << endl << "Press any key to continue..." << endl;
	std::getchar();
	return 0;
}