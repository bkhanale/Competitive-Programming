#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;

void build() {
	
}

int main() {
	string s, t;
	cin >> s >> t;
	ll m = s.length(), n = t.length();
	ll q;
	cin >> q;
	while(q--) {
		ll ai, bi, ci, di;
		cin >> ai >> bi >> ci >> di;
		build();
	}
	return 0;
}