#include <bits/stdc++.h>
using namespace std;
#define MAX 100005
#define INF INT_MAX
#define pb push_back
#define mp make_pair
typedef long long ll;

int main() {
	srand(time(NULL));
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	freopen("input.txt", "w", stdout);
	//freopen("output.txt", "w", stdout);
	//ll n = rand() % 1000 + 1, m = rand() % 1000 + 1;
	//cout << n << " " << m << endl;
	for(ll i = 0; i < 1000; i++) {
		cout << i + 1 << " ";
	}
	cout << endl;
	return 0;
}